export enum UsersRoles {
  Owner = 'owner',
  SuperAdmin = 'superadmin',
  Admin = 'admin',
  Operator = 'operator',
  Seller = 'seller',
  Auditor = 'auditor',
}

export enum ContactsRoles {
  Customer = 'customer',
  Provider = 'provider',
}
